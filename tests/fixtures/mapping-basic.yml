global:
  default_inherits: default

tools:
  default:
    cores: 2
    mem: 8
    env: {}
    scheduling:
      require: []
      prefer:
        - general
      accept:
      reject:
        - pulsar
    rules: []
  bwa:
    scheduling:
      require:
        - pulsar
  unschedulable_tool:
    scheduling:
      require:
        - non_existent
  invalidly_tagged_tool:
    scheduling:
      require:
        - general
      reject:
        - general
  regex_tool.*:
    scheduling:
      require:
        - pulsar
      reject:
        - general

destinations:
  local:
    max_accepted_cores: 4
    max_accepted_mem: 16
    tags:
      prefer:
        - general
  k8s_environment:
    max_accepted_cores: 16
    max_accepted_mem: 64
    max_accepted_gpus: 2
    tags:
      prefer:
        - pulsar
