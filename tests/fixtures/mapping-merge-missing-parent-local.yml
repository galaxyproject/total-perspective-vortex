global:
  default_inherits: default

tools:
  default:
    cores: 2
    mem: cores * 2
    env:
      TEST_JOB_SLOTS: "{cores}"
    params:
      native_spec: "--mem {mem} --cores {cores}"
  remote_missing_parent:
    inherits: missing_local_parent

destinations:
  k8s_environment:
    runner: k8s
