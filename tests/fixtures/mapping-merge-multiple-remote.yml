global:
  default_inherits: default

tools:
  default:
    cores: 2
    mem: 8
    env: {}
    rules:
      - match: input_size < 5
        fail: We don't run piddling datasets
  bwa:
    rules:
      - id: highmem_rule
        match: input_size > 40
        fail: a different kind of error
    scheduling:
      require:
        - pulsar
